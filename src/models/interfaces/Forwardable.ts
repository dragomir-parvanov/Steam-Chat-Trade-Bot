
/**
 * Going to be used when forwarding profiles.
 * @export
 * @interface Forwardable
 */
export default interface Forwardable {
    /**
     * If the the entity is sent.
     * @type {boolean}
     * @memberof Forwardable
     */
    forwarded: boolean;
}