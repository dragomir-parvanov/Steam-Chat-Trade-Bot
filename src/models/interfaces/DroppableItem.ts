export default interface DroppableItem {
  /**
   * If the item can be dropped from a normal game. 
   * Without the player need to pay anything to get that item.
   * @type {boolean}
   * @memberof ItemWithPricesModel
   */
  droppable: boolean;
}